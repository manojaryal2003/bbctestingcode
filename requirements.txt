# =============================================================================
# BBC School Management System — Regression Test Suite
# Python dependencies — install with: pip install -r requirements.txt
# =============================================================================

# Core Selenium WebDriver (Chrome automation)
selenium==4.18.1

# Test framework
pytest==8.1.1

# HTML report generation (pytest-html plugin)
pytest-html==4.1.1

# Automatic ChromeDriver management (matches installed Chrome version)
webdriver-manager==4.0.1

# Test ordering support (@pytest.mark.order decorator)
pytest-ordering==0.6

# Loads credentials from .env file into os.getenv()
python-dotenv==1.0.1
